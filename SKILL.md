---
name: blog-translate
description: 将网页博客或长文翻译成中文，默认交付保留结构、图片内嵌的 HTML 单文件。用于文章 URL 或上下文中的网页文章翻译；不用于粘贴片段、代码翻译或非文章网页。
---

# 博客译制

默认将全文译成简体中文，交付可离线阅读的自包含 HTML；用户指定其他语言或格式时相应调整以下流程。

## 获取原文

- 优先使用用户提供或上下文中明确的 URL。没有 URL 时，可用现有浏览器工具按线索定位；文章无法唯一确定或目标语言仍有歧义时再询问。
- 使用可用的网页读取或浏览器工具；已安装 `web-access` 时可使用它。需要登录态、动态渲染或静态提取不完整时，用用户已登录的浏览器。无法取得全文时说明缺失，索取可访问来源，不把局部内容当全文。
- 按文档顺序提取标题、作者、发布日期及正文结构（标题层级、段落、列表、引用、代码、配图、图注、行内链接）。链接解析为完整 URL，保留查询参数。块数异常偏少时检查嵌套容器，避免漏掉富文本正文。
- 懒加载图片先滚动加载，再从正文区块取真实图片地址；不从平台灯箱或推荐轮播取图。下载到临时 `.assets` 目录；需要登录态且无法直接下载时可截取正文配图，读图确认对应内容。视频默认用封面静帧，并在译注指向原文动态演示。

## 翻译与成文

- 按原文顺序逐块完整翻译，保留结构、层级和含义，不改成摘要。术语统一，产品名、专有名词、技术名词保留原文。
- 代码、命令、配置、目录树保持原样并放入代码块。只剥离已确认属于页面 UI 的语言标签；疑似作者误写的 `json{`、`bash#` 不擅自修正，必要时加译注。
- 保留作者的内容链接；仅确认是平台误将代码标识符（如 `$xxx`、`@xxx`）自动链接时，还原为行内代码。
- HTML 使用 [assets/template.html](assets/template.html)，按其元素说明填充并移除示例占位内容。保留 UTF-8、`lang="zh-CN"`（其他语言相应调整）、响应式排版、定义式 callout 和图片页内放大；图片写本地相对路径、描述性 `alt`，保留图注。正文链接使用 `target="_blank" rel="noopener"`。
- 中文标题与顶部来源信息对应原文，列出原文链接、作者、发布日期和“中文翻译版，仅供个人阅读参考”；原文缺失的元数据不编造。

## 交付

默认保存为项目 `outputs/YYYY-MM-DD文章名.html`。HTML 生成后执行：

```bash
python3 <skill目录>/scripts/inline_images.py <输出文件.html>
```

脚本只内嵌本地 `<img src>`，会跳过远程地址；成功退出不等于已自包含。确认正文图片均已内嵌，页面不依赖外部图片、样式或脚本。

在浏览器打开最终文件，对照原文检查内容无遗漏、图片对应且无破图、排版与移动端显示正常、链接和图片放大可用；修复后交付文件链接。无法渲染检查时明确说明未验证项。

验证完成后删除本次临时文件及 `.assets` 目录，只关闭自己创建的标签页。
