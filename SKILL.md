---
name: blog-translate
description: >-
  把网页博客、文章、长文翻译成保留原排版的中文 HTML 单文件（图片 base64 内嵌、链接可跳转、复刻博客阅读排版）。
  当用户想把一篇博客、文章、Medium 长文、长推文、微信公众号文章等网页内容转成中文版本时，使用本 skill——
  无论用户是直接给出 URL、说"这篇博客"/"这篇文章"、还是指向之前浏览过的页面。即使用户只说
  "把这篇文章翻译成中文""转成中文版""帮我译制这个链接"而没有明确要求 HTML 格式，只要目标是一篇成文的
  网页文章，都应主动使用本 skill。不适用于：翻译用户直接粘贴的纯文本片段、翻译代码或注释、
  非文章类网页（数据面板、商品页、表单）。
---

# 博客译制：网页文章 → 中文 HTML 单文件

把一篇网页博客/文章翻译成中文，产出一个**自包含的单 HTML 文件**：保留原文排版结构、
图片以 base64 内嵌（不依赖外部文件夹）、链接可跳转、复刻博客式阅读排版。

## 为什么是 HTML 单文件

最终交付物固定为单个自包含 `.html`，原因是这样最稳妥地满足"保留排版 + 图片能显示 + 链接能跳转"：

- **图片一定不破** —— base64 内嵌后图片在文件内部，转发、换设备、离线打开都不断图；
  Markdown 的相对路径一旦与图片文件夹分离就破图。
- **排版还原度高** —— HTML 能精确控制阅读栏宽度、配图居中、图注、定义块样式，
  把博客的阅读体验真正复刻出来；Markdown 的最终样子取决于阅读器。

如果用户明确要 Markdown，照办即可；但默认且推荐 HTML 单文件。

## 何时需要和用户确认

大多数情况按下面流程直接走完，不必打断用户。只在这两个点上确认：

- 用户没给 URL，且本地 Chrome 检索命中 0 个或多个候选（让用户提供 URL 或从候选中选择）。
- 目标语言不确定（默认外语 → 中文）。

## 工作流

### 1. 拿到文章 URL

默认情况：用户会直接提供博客/文章的 URL，拿到就用。

少数情况下用户只说"这篇博客""上次看的那篇"而没给 URL —— 这时用 web-access skill 的
`find-url.mjs` 按关键词检索本地 Chrome 浏览历史/书签定位：

- 命中唯一明确匹配 → 直接用。
- 命中多个候选 → 把候选（标题 + URL + 最近访问时间）列给用户挑选，不要自己拍板。
  猜错会白翻一篇，让用户花一秒确认更稳。
- 完全定位不到 → 向用户索要 URL。

### 2. 抓取正文（通过 web-access skill）

所有联网抓取都通过 web-access skill 完成（加载它并遵循其指引）。要点：

- 博客平台（X/Twitter、微信公众号、小红书、Medium 会员文等）普遍反爬或需要登录态，
  **直接用浏览器 CDP 打开**，跳过静态抓取。用户日常 Chrome 天然带登录态。
- 用 `/eval` 按**文档顺序**提取结构化内容，而不是只抓纯文本：
  - 标题、作者、发布日期；
  - 每个内容块的类型（段落 / 各级标题 / 有序列表 / 无序列表 / 引用块 / 代码块）；
  - 段落内的行内链接（链接文字 + 完整 href）—— 保留完整 URL，不裁剪查询参数。
- 富文本编辑器（DraftJS 等）常把正文包在多层"单子节点"容器里。若提取到的块数明显偏少，
  逐层钻进 `children[0]`，直到遇到真正有多个子节点的内容容器再开始提取。
- 代码块的坑：X 长文里代码块（`SECTION`）的 `innerText` 首行常是语言标签（`bash` /
  `json` / `markdown` 等），需剥掉；个别代码块作者还会把语言名误粘进代码正文（如
  `json{`、`bash#`），也要一并清理——否则 `settings.json` 之类的配置会变成无效内容、
  无法直接复制运行。代码块本身保持英文原样，只清理这类污染字符。

### 3. 抓取配图（最易错的一步）

- 图片通常懒加载：先滚动到正文中每个图片区块、等其进入视口加载，再从 DOM 取真实图片 URL。
  提取前若没滚动，部分图片尚未加载、URL 为空。
- **只从文章正文区块取图。** 平台的媒体查看器 / 灯箱（lightbox）轮播里会混入平台推荐的
  无关内容——曾经从轮播取图，结果拿到完全无关的视频。务必定位到正文里的
  `<figure>` / `<img>` / section，从那里取 `src`。
- 配图本身是视频时：取视频封面帧（`poster`）作为静帧。HTML 无法自动播放视频，做法是展示
  静帧，并在文末译注说明这些配图原为视频、可经顶部原文链接查看动态演示。
- 用 `curl` 把图片下载到一个临时的 `.assets` 文件夹（公开图床 URL 可直接下载；
  只有需要登录态才能取的资源才在浏览器内截图）。下载后读图确认内容正确，再继续。

### 4. 翻译

- **保留原排版逐块翻译**：原文是标题就译成标题，是列表就保持列表，是引用块就保持引用块，
  顺序与原文一致。
- 术语前后统一；产品名、专有名词、技术名词（如 Codex、MCP、Slack）保留原文不译。
- 定义式引用（"术语：解释"这种）→ 渲染成 callout 块，术语加粗。
- 剔除平台自动产生的垃圾链接：例如 X 会把正文里的 `$xxx`、`@xxx` 自动变成话题/账号链接，
  这并非作者本意——译文里把它们还原成行内代码 `<code>`，只保留作者真正给出的内容链接。
- 目录树、配置文件示例、命令等 → 放进代码块。

### 5. 生成 HTML

以本 skill 的 `assets/template.html` 为骨架（已内置复刻博客阅读体验的 CSS：窄阅读栏、
callout 色条、图注居中、代码块、移动端响应式）。模板顶部的注释列出了"元素词汇表"——
正文各块按原文结构映射到对应标签。填充时：

- `<h1>` 放中文标题；`<div class="meta">` 放原文链接、作者、日期、"中文翻译版"说明。
- 正文每个块对应 `<h2>` / `<p>` / `<ul>` / `<ol>` / `<blockquote class="callout">` /
  `<pre><code>`。
- 配图用 `<figure>` 直接放 `<img>`（务必写 `alt`），有图注的加 `<figcaption>`。点击图片由
  模板内置的 lightbox 在页面内放大查看，不跳转外部页面；原文出处在顶部 meta 区已有链接。
- 正文里的文字链接加 `target="_blank" rel="noopener"`，点击在新标签打开。
- 图片先写本地相对路径（如 `src="<文件名>.assets/01.jpg"`）。

### 6. 内嵌图片，做成单文件

```bash
python3 <本skill目录>/scripts/inline_images.py <输出的 .html 文件>
```

脚本把 HTML 里所有本地 `<img src>` 转成 base64 data URI，产出真正的自包含单文件。
完成后删掉临时的 `.assets` 图片文件夹——单文件已不再需要它。

### 7. 验证

用 web-access 的 CDP 打开生成的 HTML（`file://` 路径），截图确认：所有图片都正常显示、
排版正常、没有破图或错位。

### 8. 收尾

- 输出到项目的 `outputs/` 目录，文件名用"日期+名称"（如 `2026-05-21某文章.html`）。
- 关闭自己创建的浏览器 tab，保留用户原有 tab 不动。
- 删除临时文件和 `.assets` 文件夹；释放不再需要的子 agent。

## 输出规格

- 单个 `.html` 文件，自包含（图片已 base64 内嵌，零外部依赖）。
- UTF-8 编码、`lang="zh-CN"`、移动端响应式。
- 顶部 meta 区注明原文出处、作者、发布日期，并声明为中文翻译版、供个人阅读参考。
